# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build coco-fe`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t coco-fe`.
FROM docker.io/node:lts-alpine AS builder
RUN apk add g++ make py3-pip
WORKDIR /app
COPY package*.json ./
RUN ls && npm install
COPY . .
RUN npm install nx && ls && cat package.json
EXPOSE 4200
RUN npm run coco-fe:serve &


# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.

# FROM nginx:latest AS ngi
# # Copying compiled code and nginx config to different folder
# # NOTE: This path may change according to your project's output folder 
# COPY  apps/coco-fe/nginx/nginx.dev.conf  /etc/nginx/conf.d/default.conf
# # Exposing a port, here it means that inside the container 
# # the app will be using Port 80 while running
# EXPOSE 80

